services:
  phoenixd:
    image: ghcr.io/acinq/phoenixd:0.4.0
    restart: always
    ports:
      - "127.0.0.1:9740:9740"
    volumes:
      - phoenixd-data:/root/.phoenix
    command: ["--agree-to-terms-of-service", "--http-bind-ip", "0.0.0.0"]

  echo-server:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command: ["echo-server"]
    restart: always
    expose:
      - "8080"

  aperture:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    entrypoint: ["/bin/sh", "-c", "envsubst < /etc/aperture/aperture.yaml.tmpl > /etc/aperture/aperture.yaml && exec aperture --configfile=/etc/aperture/aperture.yaml"]
    restart: always
    ports:
      - "0.0.0.0:8081:8081"
    volumes:
      - ./aperture.yaml:/etc/aperture/aperture.yaml.tmpl:ro
      - aperture-data:/home/aperture/.aperture
    environment:
      - PHOENIXD_PASSWORD=${PHOENIXD_PASSWORD}
    user: "1000"
    depends_on:
      - phoenixd
      - echo-server

volumes:
  phoenixd-data:
  aperture-data:
